export type AppColorVariant = "primary" | "secondary";

export type GraphicColorResource = {
  foreground: string;
  background: string;
  shadowColor: string;
  borderColor: string;
  textColor: string;
};
